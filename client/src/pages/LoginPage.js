import React from "react";
import Header from "../Header";

export default function LoginPage() {
    return (
        <div>
            <main>
                <h1>Login</h1>
            </main>
        </div>
    );
};